@model IEnumerable<Book>
@{
    ViewData["Title"] = "Books";
}

<div class="table-responsive-sm">
    <div class="justify-content-around float-end">
        <a asp-controller="Book" asp-action="CreateBook" class="btn btn-primary">Добавить книгу</a>
    </div>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th scope="col">Название</th>
                <th scope="col">Автор</th>
                <th scope="col">Цена</th>
                <th scope="col">Описание</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Title</td>
                    <td><b>@item.Author</b></td>
                    <td>@item.Price</td>
                    <td>@item.Description</td>
                </tr>
            }
        </tbody>
    </table>
</div>
@*<a asp-controller="SearchSoftware" asp-action="GetSoftwareById" asp-route-id="@item.Id"><b>@item.Name</b></a>*@


